# 🚀 GitHub + Railway 部署完整指南

## 📋 第一步：创建GitHub仓库

### 1.1 访问GitHub创建仓库页面
👉 **立即访问：** https://github.com/new

### 1.2 填写仓库信息
```
Repository name: xiezuo-calendar
Description: 工程安装月历管理系统 - AI助手 + 实时协作
```

### 1.3 重要设置
- ✅ 选择 **Public**（公开仓库，Railway免费部署需要）
- ❌ **不要**勾选 "Add a README file"
- ❌ **不要**选择 .gitignore
- ❌ **不要**选择 License

### 1.4 点击 "Create repository"

---

## 📤 第二步：推送代码到GitHub

### 2.1 复制仓库地址
创建完成后，GitHub会显示仓库地址，例如：
```
https://github.com/your-username/xiezuo-calendar.git
```

### 2.2 告诉我您的GitHub用户名
**例如：** `WilliamInThunderBay` 或其他用户名

我会帮您执行以下命令：
```bash
# 添加远程仓库
git remote add origin https://github.com/你的用户名/xiezuo-calendar.git

# 重命名分支为main
git branch -M main

# 推送代码
git push -u origin main
```

---

## 🚂 第三步：在Railway部署

### 3.1 登录Railway
👉 **访问：** https://railway.app

点击右上角 **"Login"**，选择 **"Login with GitHub"**

### 3.2 创建新项目
1. 点击 **"New Project"**
2. 选择 **"Deploy from GitHub repo"**
3. 如果是第一次使用，会要求授权GitHub
   - 点击 **"Configure GitHub App"**
   - 选择允许访问 **`xiezuo-calendar`** 仓库

### 3.3 选择仓库并部署
1. 在列表中找到 **`xiezuo-calendar`**
2. 点击仓库名称
3. Railway会自动：
   - ✅ 检测到Node.js项目
   - ✅ 找到 `backend/package.json`
   - ✅ 开始构建和部署

---

## ⚙️ 第四步：配置环境变量

### 4.1 进入项目设置
在Railway项目页面，点击项目卡片 → **"Variables"** 标签

### 4.2 添加必需的环境变量

#### 必须配置：
```bash
NODE_ENV=production
PORT=3000
JWT_SECRET=your_jwt_secret_here_replace_this_with_random_string
```

#### 可选配置（如果需要AI功能）：
```bash
DEEPSEEK_API_KEY=your_deepseek_api_key
```

#### 生成JWT_SECRET的方法：
**方法1：** 在您的电脑上运行：
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**方法2：** 使用在线生成器：
- 访问：https://generate-secret.vercel.app/32
- 复制生成的随机字符串

### 4.3 保存并重新部署
添加环境变量后，Railway会自动重新部署。

---

## 🌐 第五步：获取访问地址

### 5.1 生成公共域名
1. 在Railway项目页面，点击 **"Settings"** 标签
2. 找到 **"Domains"** 部分
3. 点击 **"Generate Domain"**
4. Railway会生成一个URL，例如：
   ```
   https://xiezuo-calendar-production.up.railway.app
   ```

### 5.2 访问您的应用
打开生成的URL，您应该能看到：
```
https://your-app.up.railway.app/frontend/login.html
```

---

## 🎯 默认登录信息

```
用户名: admin
密码: 12345
```

**⚠️ 重要：** 登录后立即在系统中修改密码！

---

## 📊 监控和日志

### 查看部署日志
1. Railway项目页面 → **"Deployments"** 标签
2. 点击最新的部署
3. 查看实时构建日志

### 查看运行日志
1. Railway项目页面 → **"Logs"** 标签
2. 实时查看应用运行日志
3. 可以用来调试问题

---

## 🔄 后续更新流程

### 本地修改代码后更新部署：
```bash
# 1. 提交更改
git add .
git commit -m "描述你的修改"

# 2. 推送到GitHub
git push

# 3. Railway自动部署
# Railway会自动检测到GitHub的更新并重新部署
```

---

## 💰 Railway免费额度

- ✅ **$5美元/月** 免费额度
- ✅ 大约可以运行 **500小时/月**
- ✅ 足够演示和小规模使用
- ✅ 休眠机制：15分钟无访问自动休眠
- ✅ 自动唤醒：有访问时自动启动（首次需等待30秒）

---

## ❓ 常见问题

### Q1: 推送代码时要求输入密码怎么办？
**A:** GitHub已不支持密码登录，需要使用Personal Access Token：
1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 生成token并复制
5. 推送时用token代替密码

### Q2: Railway检测不到我的项目怎么办？
**A:** 需要在Railway设置中手动配置：
- Root Directory: `backend`
- Build Command: `npm install`
- Start Command: `npm start`

### Q3: 部署后访问404怎么办？
**A:** 检查：
1. Railway的端口是否设置为3000
2. 访问路径是否正确：`/frontend/login.html`
3. 查看Railway的Logs是否有错误

### Q4: WebSocket连接失败怎么办？
**A:** Railway自动支持WebSocket，但需要确保：
1. 使用WSS协议（不是WS）
2. 域名使用Railway提供的HTTPS域名

---

## 🎉 完成！

现在开始第一步：**创建GitHub仓库**

👉 访问：https://github.com/new

创建完成后，**告诉我您的GitHub用户名**，我会帮您推送代码！
